build:
  name: midcontinentcontrols/kindest
  docker: {}
#test:
  #charts:
  
  #build:
  #  docker:
  #    dockerfile: test/Dockerfile
test:
  - name: e2e
    build:
      docker:
        dockerfile: test/Dockerfile
        #context: "."
    charts:
      - path: chart/
        values: {}
    